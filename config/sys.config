[

    {kernel, [
        {logger, [
            {handler, default, logger_std_h, #{
                config    => #{type => standard_io},
                formatter => {logger_formatter, #{time_offset => "Z"}},
                level     => debug
            }}
        ]}
    ]},

    {consuela, [
        {nodename  , "consul0"},
        {namespace , <<"default">>}
    ]}

].
